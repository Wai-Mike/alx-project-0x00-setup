# Climaware Next.js Tasks (alx-project-0x00)

This folder contains the deliverables for Tasks 0–7:

- Basic structure under `pages/`, `components/`, and `interfaces/`
- Pages routing: `pages/index.tsx`, `pages/landing.tsx`, `pages/about.tsx`
- Components: `Card`, `Pill`, `Button`
- Typings in `interfaces/index.ts` and `interfaces/index.tsx`

How to run:

1. Install dependencies: `npm install`
2. Start dev server: `npm run dev -- -p 3000`
3. Visit:
   - `http://localhost:3000`
   - `http://localhost:3000/landing`
   - `http://localhost:3000/about`

Notes:
- Place `house.png` and `star.png` in `public/assets/` as referenced by `Card.tsx`.


